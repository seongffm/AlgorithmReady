#for if 문 홀수, 약수, 소수
#리스트 이용 (if문으로 담을 것들 고르고 sum함수로 합, 
평균은 round()함수로 반올림하고 전체로 나눔)
#for문 -> 테스트케이스 갯수, if문 ->비교연산자
#리스트에서 인덱스 이용해서 인덱싱 & 정렬(sort(#기본은 오름차순, reverse=True, 
lambda함수이용하여 정렬기준 정할 수도 있다.))
문자열, 정수형 형변환으로 활용가능 ((원하는거).join으로 출력 원하게)
딕셔너리, cnt 변수 이용해서 조건 심화 가능, 아스키변환, 진수변환도 있다
실수형 이용해서 원하는 자릿수까지

